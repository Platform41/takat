<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- No sandbox: Takat reads ~/.codex / ~/.claude / ~/.gemini dotfiles. -->
    <!-- Hardened runtime is applied via `codesign --options runtime`. -->
    <!-- keychain-access-groups is deferred until the DeepSeek adapter lands. -->
</dict>
</plist>
