import SwiftUI

struct SettingsView: View {
    var body: some View {
        Form {
            Text("Provider connections and refresh settings will live here.")
                .foregroundStyle(.secondary)
        }
        .padding()
        .frame(width: 420)
    }
}
