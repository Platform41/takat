# Copy to .signing.local and fill in your values.
# .signing.local is gitignored — never commit real credentials.
# export TAKAT_SIGN_ID="Developer ID Application: <TEAM NAME> (<TEAM ID>)"
# export TAKAT_NOTARY_PROFILE="takat-notary"
