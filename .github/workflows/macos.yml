# PAUSED 2026-09-07 — GitHub Actions is disabled while the repo has no CI budget.
# Verification is local for now: `swift build && swift test` on macOS 26 before every push
# (see CONTRIBUTING.md). To re-enable automatic CI, restore the `on:` block to:
#   on:
#     push:
#     pull_request:
# Until then this workflow only runs when triggered by hand from the Actions tab.

name: macOS

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-26

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build
        run: swift build

      - name: Test
        run: swift test
